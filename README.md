# Eslint

npm install
docker-compose up -d pra rodar o bd
 npm init @eslint/config@latest     
npx eslint src/app.js          pra rodar o eslint
npm install eslint-plugin-import --save-dev  -> pra ver import cycle                       
npx eslint src/app.js --fix -> pra rodar correções

baixar extensão do eslint do vs code pra ver as mudanças, o prettier, e se precisar da (ctr+p) um reload windows
e ver as mudanças 
